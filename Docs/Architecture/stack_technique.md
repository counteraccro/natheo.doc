---
title: "Stack technique"
parent: "Architecture"
nav_order: 1
---

> 🚧 **Page à réécrire pour la V2.** L'ancienne version de cette page (Symfony 7,
> Bootstrap 5, VueJS 3.3) ne reflète plus la stack actuelle. D'après le code du
> projet (`composer.json`, `package.json`, `CLAUDE.md`), la V2 s'appuie notamment sur :
>
> * PHP 8.2+ / Symfony 8.1
> * Doctrine ORM 3.x
> * Vue 3 + TypeScript, monté dans Twig via `symfony/ux-vue` (`vue_component()`)
> * Vite (`vite-plugin-symfony`) à la place de Webpack Encore
> * Tailwind CSS 4
> * MySQL 8.2+ (par défaut) ou PostgreSQL 15.2+
>
> Le détail complet (versions exactes, bundles ajoutés, librairies JS) sera repris ici
> lors de la prochaine passe de contenu.

*(à compléter)*
